# General Todos

- [ ] Add `.ez-btn-group` class name to `.ez-button-group` definitions (use `:where(...)` selector to allow use of either of the classnames as required).

## Dialog CSS component related

- [ ] Update the css component styles to change `*__content` class to `*__body`, and `*__actions` to `*__footer` classnames.
- [ ] Update the css component styles so we can apply `*__body` border-top, and footer border-top when a control class (`.with-dividers`) is added to the `.ez-dialog` class. The padding per component should also change when the `.with-dividers` class is added:
  - Header padding bottom becomes 12dp.
  - Footer padding top becomes 12dp.
  - Body padding block becomes 11dp (one pixel less than others to accommodate the space the borders (top and bottom) take up).
