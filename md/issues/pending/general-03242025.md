# General Todos

- [ ] Add `.ez-btn-group` class name to `.ez-button-group` definitions (use `:where(...)` selector to allow use of either of the classnames as required).

## Dialog CSS component related

- [ ] Update the css component styles to change `*__content` class to `*__body`, and `*__actions` to `*__footer` classnames - These make using them more intuitive.
- [ ] Update the css component styles so we can apply `*__body` border-top, and footer border-top when a control class (`.with-dividers`) is added to the `.ez-dialog` class, additionally, the padding per component should also change when the `.with-dividers` class is present:
  - Header padding bottom becomes 12dp.
  - Footer padding top becomes 12dp.
  - Body padding-block becomes 11dp (one pixel less than others to accommodate the space the added borders take up).
  - Additionally, `.ez-dialog > {dividerclass}` should be `display: none` when the `.with-dividers` class is present.
- [ ] Discourage the use of divider elements to separate the header, body, and footer, elements, and instead promote the use of the `.with-dividers` class.
- [ ] Update stories to not use `hr` elements as dividers for header, body, and footer elements.
