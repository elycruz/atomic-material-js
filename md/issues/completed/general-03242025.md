# General Todos

## Button Group related

- [x] Add `.ez-btn-group` class name to `.ez-button-group` definitions (use `:where(...)` selector to allow use of either of the classnames as required).
- [x] Add `.ez-layout-(inline|block)` to button-group styles to allow vertical layout of buttons.
  - Add storybook stories, and update existing ones to show case the vertical layout (or potentially only create a vertical layout story) - Use the approach that yields the least code and the highest [user] discoverability.

## Dialog CSS component related

- [x] Update the css component styles to change `*__content` class to `*__body`, and `*__actions` to `*__footer` classnames - These make using them more intuitive.
- [x] Update the css component styles so we can apply `*__body` border-top, and footer border-top when a control class (`.with-dividers`) is added to the `.ez-dialog` class, additionally, the padding per component should also change when the `.with-dividers` class is present:
  - Header padding bottom becomes 12dp.
  - Footer padding top becomes 12dp.
  - Body padding-block becomes 11dp (one pixel less than others to accommodate the space the added borders take up).
  - Additionally, `.ez-dialog > {dividerclass}` should be `display: none` when the `.with-dividers` class is present.
- [x] Discourage the use of divider elements to separate the header, body, and footer, elements, and instead promote the use of the `.with-dividers` class.
- [x] Update stories to not use `hr` elements as dividers for header, body, and footer elements.

## Badge

- [x] Create `ez-badge` custom element which encapsulates `.ez-badge-wrapper` functionality internally.
  - Component should additionally automatically apply position relative (to the parent element) if it doesn't have a `position` style applied.   

## Card

- [x] Ensure card classes include classes for:
  - card header
  - card body (replace '*__content' "") - Body take up remaining space in card for "block"/"vertical" card layouts.
  - card footer (replace '*__actions' "").
  - card title/headline
  - card subtitle
- [x] Update related stories to not have inline styles and instead depend on typescale classes and/or default copy/typescale/typography classes for content styling.
- [x] Update related stories to have media styling directly on `.ez-card__media` elements.
